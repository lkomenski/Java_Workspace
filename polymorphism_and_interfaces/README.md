# Bank Account Strategy Demo

Simple Java project demonstrating:

- Inheritance (`Account` → `SavingsAccount`, `CheckingAccount`)
- Polymorphism (`ArrayList<Account>`)
- Strategy Pattern (swappable interest behaviors)

Includes runtime strategy swapping and follows basic SOLID principles.

Compile and run:

javac BankDemo.java  
java BankDemo